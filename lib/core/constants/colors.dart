import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xFF1D9E75);
  static const primaryDark = Color(0xFF0F6E56);
  static const primaryLight = Color(0xFF5DCAA5);
  static const primaryGlow = Color(0x261D9E75);

  // Background
  static const bgDark = Color(0xFF080810);
  static const bgCard = Color(0xFF0F0F1A);
  static const bgSurface = Color(0xFF1A1A2E);

  // Accent
  static const purple = Color(0xFF6C3FE8);
  static const purpleGlow = Color(0x1A6C3FE8);
  static const orange = Color(0xFFFF6B35);

  // Text
  static const textPrimary = Colors.white;
  static const textSecondary = Colors.white70;
  static const textMuted = Colors.white38;

  // Borders
  static const border = Colors.white12;
  static const borderLight = Colors.white24;
}